import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.assets',
  appName: 'My Assets',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
